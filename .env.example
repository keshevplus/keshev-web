# Root .env.example for monorepo (for reference only)
# Put only shared or tooling-related variables here
# Actual secrets/configs should go in client/.env and server/.env

NODE_ENV=production
PNPM_HOME=C:\Users\youruser\AppData\Local\pnpm
