{
  "name": "keshev-web",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "postinstall": "node -e \"try { require('fs').writeFileSync('./.nvmrc', require('./package.json').engines.node.replace(/[^0-9.]/g, ''));}catch(e){console.error(e);}\"",
    "nvm:use": "nvm use",
    "start": "npx concurrently --kill-others --names CLIENT,SERVER \"pnpm --filter ./client start\" \"pnpm --filter ./server start\"",
    "start:client": "pnpm --filter ./client start",
    "start:server": "pnpm --filter ./server start",
    "build": "pnpm --recursive run build",
    "build:client": "pnpm --filter ./client build",
    "build:server": "pnpm --filter ./server build",
    "test:client": "pnpm --filter ./client test",
    "test:server": "pnpm --filter ./server test",
    "lint": "pnpm --recursive run lint",
    "lint:client": "pnpm --filter ./client lint",
    "lint:server": "pnpm --filter ./server lint",
    "deploy": "cd client && pnpm run build && cd .. && git add . && git commit -m \"Deploy: Production build update\" && git push origin main",
    "check-env-vars": "pnpm node scripts/check-env-vars.js"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/keshevplus/keshev-web.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/keshevplus/keshev-web/issues"
  },
  "homepage": "https://github.com/keshevplus/keshev-web",
  "engines": {
    "node": "18.19.1",
    "npm": "9.x",
    "pnpm": ">=8"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0"
  }
}
